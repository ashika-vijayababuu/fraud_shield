apiVersion: 1

providers:
  - name: FraudShield Dashboards
    orgId: 1
    folder: FraudShield
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
